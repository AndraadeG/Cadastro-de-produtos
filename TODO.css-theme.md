# TODO - Tema Dark (Chumbo) e Fundo Animado

## Passos
1. Atualizar `index.css` para dark theme (chumbo escuro) via variáveis CSS.
2. Trocar `body` para fundo escuro com gradiente + animação sutil.
3. Ajustar `branding`, `.card`, inputs/selects, textos (`label`, `--muted`, etc.) para manter contraste.
4. Ajustar `.tips` e estilos relacionados.
5. Ajustar botões/links (`.btn`, links dentro do card) para aparência consistente.
6. Atualizar `produtos.php` removendo cores inline fixas do header/linhas do `<table>` e usando variáveis CSS.
7. Verificar páginas: `index.php`, `cadastrar_produto.php`, `editar-produto.php`, `produtos.php`.

## Progresso
- index.css: tema dark (chumbo escuro) + fundo animado aplicado.
- branding/cards/inputs/textos: contraste ajustado.
- produtos.php: cabeçalho da tabela ajustado para dark.


